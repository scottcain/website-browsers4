
user  www-data;
worker_processes  5;
daemon off;
#keepalive_timeout  2;

# Error log; other directives: notice | info
error_log  /usr/local/wormbase/logs/nginx-error.log warn;

# pid file (defaults to logs)
pid        /usr/local/wormbase/services/nginx/logs/nginx.pid;

events {
    worker_connections  1024;
}


http {
    server_names_hash_bucket_size 64;

    # Optional: set up the access log and define a format
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';
    access_log /usr/local/wormbase/logs/nginx-access.log main;

    # log for cache hits.
   log_format cache '***$time_local '
	            '$upstream_cache_status '
                    'Cache-Control: $upstream_http_cache_control '
                    'Expires: $upstream_http_expires '
                    '"$request" ($status) '
		    '"$http_user_agent" ';


 
    # Load-balancing against a pool of fastcgi TCP sockets.
    # Simple load-balancing.
    # Define as many servers as we like (and weight them)

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;


#############################################
#
#  nagios:8080
#
#############################################
server {
       listen   8080;
       server_name  dev.wormbase.org;
 
       access_log  /usr/local/wormbase/logs/nagios-acccess.log;
 
       location / {
 	     root   /usr/local/wormbase/services/nagios/share;
	     index index.php;
#	     fastcgi_index  index.php;
#index  index.php;
#             fastcgi_pass   127.0.0.1:9000;
	     rewrite	  ^/nagios/images/(.*) /images/$1 break;
	     rewrite	  ^/nagios/stylesheets/(.*) /stylesheets/$1 break;
#             auth_basic	  "Restricted";
#             auth_basic_user_file	conf/htpasswd;
	}


        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        #
        location ~ \.php$ {
 	    root   /usr/local/wormbase/services/nagios/share;
            fastcgi_pass   127.0.0.1:9000;
            fastcgi_index  index.php;
            fastcgi_param  SCRIPT_FILENAME  /usr/local/wormbase/services/nagios/share$fastcgi_script_name;
	    include /usr/local/wormbase/services/nginx/conf/fastcgi_params;
        }
 
       location ~ \.cgi$ {
 	     root	/usr/local/wormbase/services/nagios/sbin;
	     rewrite	^/nagios/cgi-bin/(.*)\.cgi /$1.cgi break;
 
	     include /usr/local/wormbase/services/nginx/conf/fastcgi_params;
             fastcgi_pass   127.0.0.1:49233;
             fastcgi_param  SCRIPT_FILENAME  /usr/local/wormbase/services/nagios/sbin$fastcgi_script_name;  # same path as above
 
#	     auth_basic		"Restricted";
#	     auth_basic_user_file	conf/htpasswd;
 
#	     fastcgi_param  AUTH_USER     $remote_user;
#            fastcgi_param  REMOTE_USER	  $remote_user;
       }
}



#############################################
#
#  staging.wormbase.org, nginx port 80, starman 5000
#
#############################################

server {
      server_name staging.wormbase.org;
      listen      80;

      #charset koi8-r;
      #access_log  logs/host.access.log  main;
		       
       access_log /usr/local/wormbase/website/staging/logs/cache.log cache;
       access_log /usr/local/wormbase/website/staging/logs/access.log main;
       # Error log; other directives: notice | info
       error_log  /usr/local/wormbase/website/staging/logs/error.log warn;

       # Enable gzip compression
       gzip             on;
       gzip_min_length  1000;
       gzip_proxied     any;
       gzip_types       text/xml text/plain application/xml;

       include       mime.types;
       default_type  application/octet-stream;

       #error_page  404              /404.html;
       # redirect server error pages to the static page /50x.html
       #
#       error_page   500 502 503 504  /50x.html;
#       location = /50x.html {
#           root   html;
#       }

       # Show status information on /status
       location = /status {
           stub_status on;
           allow 127.0.0.1;
           deny all;
       }


       location / {

            ########################################
            #  Pool of fastcgi sockets
            ######################################## 
#           include fastcgi_params; # We'll discuss this later
            # Unix socket
            # fastcgi_pass unix:/tmp/myapp.fcgi;
   
            # TCP socket
            # fastcgi_pass  127.0.0.1:10004;
	  
            # Or load-balanced pool (currently of one)
#           fastcgi_pass wormbase;

            ########################################
            #  Pool of PSGI/Starman hosts
            ######################################## 
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            # proxy_set_header X-Forwarded-Port 443; #this is important for Catalyst Apps!
           
	    # Proxy pass to a single server for testing:
	    proxy_pass http://localhost:5000;

#           proxy_redirect   off;
   	    client_max_body_size            0;
            client_body_buffer_size         128k;
	    proxy_send_timeout              120;
	    proxy_buffer_size               4k;
	    proxy_buffers                   4 32k;
  	    proxy_busy_buffers_size         64k;
 	    proxy_temp_file_write_size      64k;
 	    proxy_connect_timeout           75;
  	    proxy_read_timeout              205;

  	# Back end server configuration
  #            root   html;
  #            index  index.html index.htm;

           # Caching diretives
#          proxy_cache_bypass $cookie___ac;
#          proxy_cache thecache;
#          proxy_cache_key $scheme$proxy_host$request_uri;
        }

	# TO BE CONFIGURED
        location /static {
            root  /usr/local/wormbase/website/staging/root;
        }
    }



#############################################
#
#  dev.wormbase.org
#
#  Listening on 80, proxy pass to 8000
#
#############################################

  server {
      server_name dev.wormbase.org;
      listen      80;

      #charset koi8-r;
      #access_log  logs/host.access.log  main;
		       
       access_log /usr/local/wormbase/logs/classic-nginx-cache.log cache;
       access_log /usr/local/wormbase/logs/classic-nginx-access.log main;
       # Error log; other directives: notice | info
       error_log  /usr/local/wormbase/logs/classic-nginx-error.log warn;

       # Enable gzip compression
       gzip             on;
       gzip_min_length  1000;
       gzip_proxied     any;
       gzip_types       text/xml text/plain application/xml;

       include       mime.types;
       default_type  application/octet-stream;

       #error_page  404              /404.html;
       # redirect server error pages to the static page /50x.html
       #
#       error_page   500 502 503 504  /50x.html;
#       location = /50x.html {
#           root   html;
#       }

       # Show status information on /status
       location = /status {
           stub_status on;
           allow 127.0.0.1;
           deny all;
       }


       location / {

            ########################################
            #  Pool of fastcgi sockets
            ######################################## 
#           include fastcgi_params; # We'll discuss this later
            # Unix socket
            # fastcgi_pass unix:/tmp/myapp.fcgi;
   
            # TCP socket
            # fastcgi_pass  127.0.0.1:10004;
	  
            # Or load-balanced pool (currently of one)
#           fastcgi_pass wormbase;


            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            # proxy_set_header X-Forwarded-Port 443; #this is important for Catalyst Apps!
           
	    # Proxy pass to a single server for testing:
	    proxy_pass http://localhost:8000;

#           proxy_redirect   off;
   	    client_max_body_size            0;
            client_body_buffer_size         128k;
	    proxy_send_timeout              120;
	    proxy_buffer_size               4k;
	    proxy_buffers                   4 32k;
  	    proxy_busy_buffers_size         64k;
 	    proxy_temp_file_write_size      64k;
 	    proxy_connect_timeout           75;
  	    proxy_read_timeout              205;

  	# Back end server configuration
  #            root   html;
  #            index  index.html index.htm;

           # Caching diretives
#          proxy_cache_bypass $cookie___ac;
#          proxy_cache thecache;
#          proxy_cache_key $scheme$proxy_host$request_uri;
        }

        location /static {
            root  /usr/local/wormbase/website/development/root;
        }
    }


#############################################
#
#  todd.wormbase.org; starman on 9001
#
#############################################
 server {
      server_name todd.wormbase.org;
      listen      80;

      #charset koi8-r;
      #access_log  logs/host.access.log  main;
		       
       access_log /usr/local/wormbase/website/tharris/logs/cache.log cache;
       access_log /usr/local/wormbase/website/tharris/logs/access.log main;
       # Error log; other directives: notice | info
       error_log  /usr/local/wormbase/website/tharris/logs/error.log warn;

       # Enable gzip compression
       gzip             on;
       gzip_min_length  1000;
       gzip_proxied     any;
       gzip_types       text/xml text/plain application/xml;

       include       mime.types;
       default_type  application/octet-stream;

       #error_page  404              /404.html;
       # redirect server error pages to the static page /50x.html
       #
#       error_page   500 502 503 504  /50x.html;
#       location = /50x.html {
#           root   html;
#       }

       # Show status information on /status
       location = /status {
           stub_status on;
           allow 127.0.0.1;
           deny all;
       }


       location / {

            ########################################
            #  Pool of fastcgi sockets
            ######################################## 
#           include fastcgi_params; # We'll discuss this later
            # Unix socket
            # fastcgi_pass unix:/tmp/myapp.fcgi;
   
            # TCP socket
            # fastcgi_pass  127.0.0.1:10004;
	  
            # Or load-balanced pool (currently of one)
#           fastcgi_pass wormbase;

            ########################################
            #  Pool of PSGI/Starman hosts
            ######################################## 
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            # proxy_set_header X-Forwarded-Port 443; #this is important for Catalyst Apps!
           
	    # Proxy pass to a single server for testing:
	    proxy_pass http://localhost:9001;

#           proxy_redirect   off;
   	    client_max_body_size            0;
            client_body_buffer_size         128k;
	    proxy_send_timeout              120;
	    proxy_buffer_size               4k;
	    proxy_buffers                   4 32k;
  	    proxy_busy_buffers_size         64k;
 	    proxy_temp_file_write_size      64k;
 	    proxy_connect_timeout           75;
  	    proxy_read_timeout              205;

  	# Back end server configuration
  #            root   html;
  #            index  index.html index.htm;

           # Caching diretives
#          proxy_cache_bypass $cookie___ac;
#          proxy_cache thecache;
#          proxy_cache_key $scheme$proxy_host$request_uri;
        }

        location /static {
            root  /usr/local/wormbase/website/tharris/root;
        }
    }



#############################################
#
#  abby.wormbase.org ; starman 9003
#
#############################################
 server {
      server_name abby.wormbase.org;
      listen 80;
		       
      access_log /usr/local/wormbase/website/acabunoc/logs/cache.log cache;
      access_log /usr/local/wormbase/website/acabunoc/logs/access.log main;
      # Error log; other directives: notice | info
      error_log  /usr/local/wormbase/website/acabunoc/logs/error.log warn;

      # Enable gzip compression
      gzip             on;
      gzip_min_length  1000;
      gzip_proxied     any;
      gzip_types       text/xml text/plain application/xml;

      include       mime.types;
      default_type  application/octet-stream;

      # Show status information on /status
      location = /status {
          stub_status on;
          allow 127.0.0.1;
          deny all;
      }


      location / {

            ########################################
            #  Pool of PSGI/Starman hosts
            ######################################## 
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            # proxy_set_header X-Forwarded-Port 443; #this is important for Catalyst Apps!
           
	    # Proxy pass to a single server for testing:
	    proxy_pass http://localhost:9003;

#           proxy_redirect   off;
   	    client_max_body_size            0;
            client_body_buffer_size         128k;
	    proxy_send_timeout              120;
	    proxy_buffer_size               4k;
	    proxy_buffers                   4 32k;
  	    proxy_busy_buffers_size         64k;
 	    proxy_temp_file_write_size      64k;
 	    proxy_connect_timeout           75;
  	    proxy_read_timeout              205;
       }

       location /static {
            root  /usr/local/wormbase/website/acabunoc/root;
        }
    }



#############################################
#
#  xshi.wormbase.org; starman on 9004
#
#############################################
 server {
      server_name xshi.wormbase.org;
      listen 80;
		       
      access_log /usr/local/wormbase/website/xshi/logs/cache.log cache;
      access_log /usr/local/wormbase/website/xshi/logs/access.log main;
      # Error log; other directives: notice | info
      error_log  /usr/local/wormbase/website/xshi/logs/error.log warn;

      # Enable gzip compression
      gzip             on;
      gzip_min_length  1000;
      gzip_proxied     any;
      gzip_types       text/xml text/plain application/xml;

      include       mime.types;
      default_type  application/octet-stream;

      # Show status information on /status
      location = /status {
          stub_status on;
          allow 127.0.0.1;
          deny all;
      }


      location / {
            ########################################
            #  Pool of PSGI/Starman hosts
            ######################################## 
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            # proxy_set_header X-Forwarded-Port 443; #this is important for Catalyst Apps!
           
	    # Proxy pass to a single server for testing:
	    proxy_pass http://localhost:9004;

#           proxy_redirect   off;
   	    client_max_body_size            0;
            client_body_buffer_size         128k;
	    proxy_send_timeout              120;
	    proxy_buffer_size               4k;
	    proxy_buffers                   4 32k;
  	    proxy_busy_buffers_size         64k;
 	    proxy_temp_file_write_size      64k;
 	    proxy_connect_timeout           75;
  	    proxy_read_timeout              205;

  	# Back end server configuration
  #            root   html;
  #            index  index.html index.htm;

        }

        location /static {
            root  /usr/local/wormbase/website/xshi/root;
        }
    }



#############################################
#
#  norie.wormbase.org; starman on 9002
#
#############################################
 server {
      server_name norie.wormbase.org;
      listen 80;
		       
      access_log /usr/local/wormbase/website/norie/logs/cache.log cache;
      access_log /usr/local/wormbase/website/norie/logs/access.log main;
      # Error log; other directives: notice | info
      error_log  /usr/local/wormbase/website/norie/logs/error.log warn;

      # Enable gzip compression
      gzip             on;
      gzip_min_length  1000;
      gzip_proxied     any;
      gzip_types       text/xml text/plain application/xml;

      include       mime.types;
      default_type  application/octet-stream;

      # Show status information on /status
      location = /status {
           stub_status on;
           allow 127.0.0.1;
           deny all;
       }

       location / {

            ########################################
            #  Pool of PSGI/Starman hosts
            ######################################## 
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            # proxy_set_header X-Forwarded-Port 443; #this is important for Catalyst Apps!
           
	    # Proxy pass to a single server for testing:
	    proxy_pass http://localhost:9002;

#           proxy_redirect   off;
   	    client_max_body_size            0;
            client_body_buffer_size         128k;
	    proxy_send_timeout              120;
	    proxy_buffer_size               4k;
	    proxy_buffers                   4 32k;
  	    proxy_busy_buffers_size         64k;
 	    proxy_temp_file_write_size      64k;
 	    proxy_connect_timeout           75;
  	    proxy_read_timeout              205;

  	# Back end server configuration
  #            root   html;
  #            index  index.html index.htm;

           # Caching diretives
#          proxy_cache_bypass $cookie___ac;
#          proxy_cache thecache;
#          proxy_cache_key $scheme$proxy_host$request_uri;
        }

        location /static {
            root  /usr/local/wormbase/website/norie/root;
        }
    }





}
